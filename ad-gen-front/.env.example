# API endpoint for the backend. Leave empty to use the same origin as the frontend.
# Example: http://localhost:3000
VITE_API_URL=

# API key sent with every request to the backend. Override this in production.
VITE_API_KEY=dev-test-key-12345
